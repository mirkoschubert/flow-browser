const Screen = require('./core/screen');
const Bindings = require('./core/bindings');

class App {
  constructor() {
    this.config = {};
    this.bindings = {};
    this.events = {};
    this.ui = {};
  }
}

module.exports = App;
